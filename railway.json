{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py migrate --noinput && python manage.py collectstatic --noinput && sleep 15 && gunicorn myproject.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4",
        "restartPolicyType": "ON_FAILURE"
    }
}