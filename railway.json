{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "gunicorn myproject.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4",
        "healthcheckPath": "/health/",
        "restartPolicyType": "ON_FAILURE"
    }
}